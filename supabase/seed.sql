-- Seed template for Couples Wealth Master
-- Run in SQL editor AFTER replacing with real couple emails:

-- insert into public.allowed_emails (email) values
--   ('husband@example.com'),
--   ('wife@example.com')
-- on conflict (email) do nothing;

-- Users are normally created via register_couple_user() on first login.
